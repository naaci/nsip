# Reading the implementation part of the package.
#

# SetInfoLevel(InfoIntPart,1);

ReadPackage( "nsip", "gap/numericalsets.gi" );
ReadPackage( "nsip", "gap/integerpartitions.gi" );
ReadPackage( "nsip", "gap/conversions.gi" );
ReadPackage( "nsip", "gap/common.gi" );

ReadPackage( "nsip", "gap/representation.gi" );

ReadPackage( "nsip", "gap/apery.gi" );

ReadPackage( "nsip", "gap/n.gi" );
ReadPackage( "nsip", "gap/d.gi" );

ReadPackage( "nsip", "gap/random.gi" );

ReadPackage( "nsip", "gap/tree.gi" );

ReadPackage( "nsip", "gap/hooklengths.gi" );

ReadPackage( "nsip", "gap/special.gi" );

ReadPackage( "nsip", "gap/euler.gi" );
